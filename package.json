{
  "name": "checkin-quality-tool",
  "private": true,
  "version": "2.5.9",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b",
    "test:reminder": "rm -rf /tmp/checkin-reminder-tests && npx tsc --ignoreConfig --target ES2022 --module ESNext --moduleResolution Bundler --outDir /tmp/checkin-reminder-tests --skipLibCheck src/worker/reminderListParser.ts src/worker/reminderMatching.ts src/worker/reminderAppealParser.ts src/worker/reminderProjectGroup.ts src/worker/reminderConfig.ts src/worker/excelReader.ts src/worker/progress.ts src/worker/whitelist.ts src/worker/utils.ts src/worker/sheetjs.d.ts && perl -0pi -e 's/from \"(\\.\\/[^\".]+)\"/from \"$1.js\"/g' /tmp/checkin-reminder-tests/worker/*.js && mkdir -p /tmp/checkin-reminder-tests/scripts && cp scripts/reminder-logic.test.mjs /tmp/checkin-reminder-tests/scripts/reminder-logic.test.mjs && node --test /tmp/checkin-reminder-tests/scripts/reminder-logic.test.mjs"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "lightningcss": "^1.32.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
